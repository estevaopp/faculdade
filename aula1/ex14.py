# 14) Faça um algoritmo onde deverá ser informado a base e a altura de um triângulo. O algoritmo
# deverá exibir o valor da área.

base = float(input("informe a base do triangulo: "))

altura = float(input("informe a altura do triangulo: "))

area = (base * altura)/2

print(f"area = {area}")